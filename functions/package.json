{
  "name": "echoes-functions",
  "private": true,
  "description": "Firebase Cloud Functions for ECHOES server-side workflows.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check index.js && node --test",
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {}
}
